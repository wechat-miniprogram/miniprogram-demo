
<view class="page" data-weui-theme="{{theme}}">
  <view
    class="canvas-wrap" style="width: {{widthScale * 100}}%; height: {{heightScale * 100}}%"
  >
    <xr-frame-canvas
      disable-scroll
      id="canvas"
      width="{{width}}"
      height="{{height}}"
      style="width:{{domWidth}}px;height:{{domHeight}}px;"
      bind:sceneReady="handleXRSceneReady"
    ></xr-frame-canvas>

  </view>

  <view class="hint-words">提示：摄像头对准人体</view>

  <!-- <view class="wrap-option">
    <button type="primary" bindtap="switchCamera">切换前后置摄像头</button>
  </view> -->
</view>

